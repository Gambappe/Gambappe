# Raw HISTORICAL corpus is re-fetchable and large — never committed. The LIVE corpus is
# capture-once evidence (scores at capture time are unreproducible) and small, so it IS
# committed, as are derived artifacts (skills, training report, odds history).
data/*
!data/skills/
!data/live/
!data/lebron-2026/
